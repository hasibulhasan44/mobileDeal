import React from 'react';

const Loading = () => {
    return (
        <div className='h-[500px]'>
            <button className='items-center btn btn-circle loading w-14 flex justify-center mx-auto'></button>
        </div>
    );
};

export default Loading;